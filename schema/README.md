# Evidence schemas

The schemas use JSON Schema draft 2020-12. Schema v2 describes active GitHub
Actions command results, manifests, suite summaries, raw indexes, failures, and
bounded binary inventories.

The two v1 schemas remain only to identify historical CircleCI bundles. Those
bundles are retained unchanged and are not produced or rewritten by version
0.2.0.
